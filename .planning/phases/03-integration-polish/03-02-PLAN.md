---
id: "03-02"
phase: 3
plan: 2
wave: 2
title: "UI Polish + README"
objective: "Fix PT-BR accent issues in button text, add title/logo area, verify error handling, and write a PT-BR README"
files_modified:
  - "public/index.html"
  - "README.md"
task_count: 2
autonomous: true
depends_on:
  - "03-01"
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - "Fetch button reads 'Extrair Pagina' with correct cedilla and accent"
    - "Export button reads 'Gerar Pagina Afiliado' with correct accent and the download emoji"
    - "Header subtitle uses correct PT-BR accents throughout"
    - "A descriptive subtitle appears below the app title in the header"
    - "If /api/fetch returns a non-2xx response the error banner becomes visible"
    - "README.md exists and covers: what it is, npm install, node server.js, step-by-step usage"
  artifacts:
    - path: "public/index.html"
      provides: "Polished frontend with correct PT-BR text and improved header"
    - path: "README.md"
      provides: "PT-BR setup and usage instructions"
  key_links:
    - from: "public/index.html btn-fetch click handler"
      to: "error-banner div"
      via: "showError() called on response.ok === false"
      pattern: "showError"
---

<objective>
UI polish pass on public/index.html and authoring of README.md.

Purpose: Ship the product in a state the user can hand to an affiliate colleague
without embarrassment — correct Portuguese text with accents, a clear header identity,
visible error feedback, and a README that explains setup in under two minutes.

Output:
- public/index.html — button labels corrected, header subtitle improved, error handling verified
- README.md — PT-BR instructions: what it is, install, run, step-by-step usage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@public/index.html

## Current text issues in public/index.html

The following strings in public/index.html are missing PT-BR accents/cedillas.
Fix each one exactly as listed (do not change surrounding HTML structure):

| Location | Current text | Correct text |
|----------|-------------|-------------|
| btn-fetch span.btn-label (line ~303) | Extrair Pagina | Extrair Pagina with acute accent on a: Extrair Página |
| btn-export textContent (line ~629 in JS finally block) | Gerar Pagina Afiliado | ⬇️ Gerar Página Afiliado |
| btn-export initial label in HTML (line ~380) | Gerar Pagina Afiliado | ⬇️ Gerar Página Afiliado |
| app-header subtitle p tag (line ~282) | Clone qualquer pagina VSL e gere sua versao de afiliado em menos de 1 minuto | Clone qualquer página VSL e gere sua versão de afiliado em menos de 1 minuto |
| section-fetch card-title (line ~290) | A — Pagina VSL para Clonar | A — Página VSL para Clonar |
| label for url-input (line ~292) | URL da pagina VSL | URL da página VSL |
| section-fetch showError call when url empty (line ~549 in JS) | Digite a URL da pagina VSL antes de extrair. | Digite a URL da página VSL antes de extrair. |
| section-header card-title (line ~329) | B — Scripts de Header do Afiliado | B — Scripts de Header do Afiliado (no change needed — already correct) |
| section-player label (line ~357) | Codigo de embed do seu player | Código de embed do seu player |
| section-checkout card-title (line ~368) | D — Links de Checkout do Afiliado | D — Links de Checkout do Afiliado (no change needed) |
| btnExport loading text in JS (line ~628) | Gerando pagina... | Gerando página... |
| fetch catch error message (line ~607) | Falha de conexao com o servidor. Verifique se o servidor esta rodando. | Falha de conexão com o servidor. Verifique se o servidor está rodando. |
| export catch error message (line ~663) | Falha de conexao ao exportar. Verifique se o servidor esta rodando. | Falha de conexão ao exportar. Verifique se o servidor está rodando. |
| renderCheckoutInputs: "Sem Deteccao" label (line ~469) | Sem Deteccao | Sem Detecção |
| valVsl "Nao" text (line ~589) | Nao | Não |

## Error handling verification

The error banner is already wired: showError() sets fetchError.textContent and adds
class "visible". The CSS shows .error-banner.visible as display:block. The fetch
handler calls showError() on response.ok === false and on network catch. No code
change is needed for error handling — just verify it is intact after the text edits.

## Header improvement

Current header p tag: "Clone qualquer pagina VSL e gere sua versao de afiliado em menos de 1 minuto"
After accent fix this becomes: "Clone qualquer página VSL e gere sua versão de afiliado em menos de 1 minuto"
This is sufficient — no structural change to the header is required beyond the text correction.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all PT-BR accent/cedilla issues in public/index.html</name>
  <files>public/index.html</files>
  <action>
    Read public/index.html in full first, then apply every text correction listed in
    the context table above. Make surgical edits — change only the text strings, do not
    alter surrounding HTML attributes, class names, IDs, or JavaScript logic.

    Priority fixes (most visible to users):

    1. btn-fetch label: change "Extrair Pagina" to "Extrair Página"
    2. btn-export HTML label: change "Gerar Pagina Afiliado" to "⬇️ Gerar Página Afiliado"
    3. btn-export JS finally reset: change "Gerar Pagina Afiliado" to "⬇️ Gerar Página Afiliado"
    4. app-header subtitle: fix "pagina" to "página" and "versao" to "versão"
    5. section-fetch card-title: fix "Pagina" to "Página"
    6. URL label: fix "pagina" to "página"
    7. Empty URL error message: fix "pagina" to "página"
    8. Player embed label: fix "Codigo" to "Código"
    9. Export loading text in JS: fix "pagina" to "página"
    10. Network error messages: fix "conexao" to "conexão" and "esta" to "está"
    11. "Sem Deteccao" in renderCheckoutInputs: fix to "Sem Detecção"
    12. "Nao" in vslDetected false branch: fix to "Não"

    After edits, verify the file is valid HTML: the DOCTYPE, head, and body structure
    must be intact, and the inline script block must not have any syntax breaks.

    Quick syntax check after saving:
      node -e "const h=require('fs').readFileSync('./public/index.html','utf8'); if(!h.includes('</html>'))throw new Error('truncated'); console.log('HTML intact');"
  </action>
  <verify>
    node -e "
      const h = require('fs').readFileSync('./public/index.html','utf8');
      const checks = [
        ['Extrair P\u00e1gina', 'btn-fetch label'],
        ['\u2b07\ufe0f Gerar P\u00e1gina Afiliado', 'btn-export label'],
        ['vers\u00e3o', 'header subtitle'],
        ['C\u00f3digo de embed', 'player label'],
        ['Sem Detec\u00e7\u00e3o', 'checkout empty label'],
        ['N\u00e3o', 'vsl not detected'],
        ['conex\u00e3o', 'error message'],
      ];
      const fails = checks.filter(([text]) => !h.includes(text));
      if (fails.length) {
        console.error('Missing accented strings:', fails.map(([,name])=>name));
        process.exit(1);
      }
      if (!h.includes('</html>')) { console.error('HTML truncated'); process.exit(1); }
      console.log('All PT-BR accent fixes verified OK');
    "
  </verify>
  <done>
    The inline node check above exits 0. All 12 accent/cedilla corrections are present.
    The file is valid HTML with intact script block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write README.md in PT-BR</name>
  <files>README.md</files>
  <action>
    Create /Users/victorroque/Downloads/Extrator2000/README.md in PT-BR.

    The README must cover exactly:

    1. What it is — one paragraph: VSL Cloner is a local Node.js tool for digital
       affiliates that clones any VSL page, removes original tracking and player scripts,
       and injects the affiliate's own pixel, VTURB player, and checkout links, generating
       a ready-to-deploy HTML file. Runs locally on port 3000.

    2. Requisitos — Node.js 18+ (no other runtime or global install needed).

    3. Instalacao — two commands:
         git clone <repo> && cd <repo>  (or: download and unzip)
         npm install

    4. Iniciar — one command:
         node server.js
       Confirm: browser at http://localhost:3000

    5. Como usar (step by step, numbered):
       1. Cole a URL da pagina VSL no campo e clique "Extrair Pagina"
       2. Aguarde o resumo aparecer: scripts removidos, VSL detectado, links de checkout encontrados
       3. No campo "Pixel & Scripts de Rastreamento", cole seu Meta Pixel ou GTM
       4. No campo "Script VTURB / Preload", cole o script de preload do seu player
       5. No campo "Embed do Player VTURB", cole o codigo de embed completo do seu player
       6. Nos inputs de Checkout, substitua os links pelo seu link de afiliado correspondente
       7. Clique "Gerar Pagina Afiliado" — o arquivo pagina-afiliado.html sera baixado automaticamente

    6. Plataformas suportadas — bullet list:
       - Checkout: ClickBank, Hotmart, Kiwify, Eduzz, Monetizze
       - Player: VTURB / Smartplayer
       - Pixel: Meta (Facebook) Pixel, Google Tag Manager

    7. Notas tecnicas — short bullets:
       - O HTML da pagina original nunca e salvo em disco — fica apenas em memoria durante a sessao
       - A ferramenta roda inteiramente local; nenhum dado e enviado a servidores externos
       - Testado com Node.js 18 e 20

    Write in natural PT-BR (with proper accents). Keep it short — the whole README
    should be under 80 lines. No badges, no license section, no English.
  </action>
  <verify>
    node -e "
      const h = require('fs').readFileSync('./README.md','utf8');
      const checks = ['npm install','node server.js','localhost:3000','ClickBank','Hotmart','VTURB','pagina-afiliado.html'];
      const missing = checks.filter(c => !h.includes(c));
      if (missing.length) { console.error('README missing:', missing); process.exit(1); }
      if (h.split('\n').length > 90) { console.error('README too long:', h.split('\n').length, 'lines'); process.exit(1); }
      console.log('README OK');
    "
  </verify>
  <done>
    README.md exists, the inline node check exits 0, all key phrases are present,
    and the file is under 90 lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | This plan only edits static HTML/text and creates a README |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | public/index.html text edits | accept | Edits are purely cosmetic text changes to static labels; no logic or input handling is modified |
</threat_model>

<verification>
After both tasks complete:

1. node -e "const h=require('fs').readFileSync('./public/index.html','utf8'); ['Extrair P\u00e1gina','\u2b07\ufe0f Gerar P\u00e1gina Afiliado','vers\u00e3o'].forEach(s=>{if(!h.includes(s))throw new Error('missing: '+s)}); console.log('UI text OK');"
2. node -e "const h=require('fs').readFileSync('./README.md','utf8'); ['npm install','node server.js','pagina-afiliado.html'].forEach(s=>{if(!h.includes(s))throw new Error('missing: '+s)}); console.log('README OK');"
3. Open http://localhost:3000 in browser and confirm button labels display with correct accents (checkpoint — visual verification)
</verification>

<success_criteria>
- Fetch button displays "Extrair Página" (with accent)
- Export button displays "⬇️ Gerar Página Afiliado" (with accent and emoji)
- All PT-BR text in the UI uses proper accents and cedillas
- Error banner appears when the server returns an error (existing wiring intact)
- README.md is present, in PT-BR, covers install + run + usage in under 90 lines
</success_criteria>

<output>
After completion, create .planning/phases/03-integration-polish/03-02-SUMMARY.md
following the standard summary template.
</output>
