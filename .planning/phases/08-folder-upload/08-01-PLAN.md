---
phase: 08-folder-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - server.js
autonomous: true
requirements:
  - UPLOAD-02
  - UPLOAD-03
  - UPLOAD-04
must_haves:
  truths:
    - "POST /api/upload-folder aceita multipart com files[] + paths[], identifica index.html, roda pipeline cleanHtml/detect*, retorna mesmo JSON que /api/fetch + uploadSessionId"
    - "POST /api/upload-folder retorna 400 se index.html nao estiver na raiz da pasta"
    - "POST /api/export-zip com uploadSessionId le assets do sessionStore em vez de baixar da rede, preservando estrutura de diretorios no ZIP"
    - "Arquivos com extensoes nao permitidas sao rejeitados silenciosamente pelo fileFilter"
    - "Caminhos com path traversal (../) sao descartados pelo isSafeRelativePath"
    - "sessionStore limpa sessoes expiradas a cada 5 minutos (TTL 30 min)"
    - "Fluxo existente /api/fetch e /api/export-zip com pageUrl continua funcionando sem alteracao"
  artifacts:
    - path: "server.js"
      provides: "multer config, sessionStore Map, /api/upload-folder route, export-zip uploadSessionId branch, multer error handler"
      contains: "api/upload-folder"
    - path: "package.json"
      provides: "multer dependency"
      contains: "multer"
  key_links:
    - from: "/api/upload-folder"
      to: "sessionStore"
      via: "crypto.randomUUID() key, assets Map stored"
      pattern: "sessionStore\\.set"
    - from: "/api/export-zip"
      to: "sessionStore"
      via: "uploadSessionId lookup, assets read into archive"
      pattern: "sessionStore\\.get\\(uploadSessionId\\)"
    - from: "/api/upload-folder"
      to: "cleanHtml + detectCheckoutLinks + detectBundleImages + detectVturbDelay"
      via: "same pipeline as /api/fetch"
      pattern: "detectVturbDelay.*cleanHtml.*detectCheckoutLinks.*detectBundleImages"
---

<objective>
Adicionar ao server.js a infraestrutura de upload de pasta: dependencia multer, session store em memoria, rota POST /api/upload-folder que processa index.html com o mesmo pipeline de /api/fetch, e branch no /api/export-zip para ler assets do session store quando uploadSessionId estiver presente.

Purpose: Habilitar o backend para receber pastas HTML locais como alternativa ao fetch por URL, mantendo pipeline identico.
Output: server.js com nova rota funcional + package.json com multer instalado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-folder-upload/08-CONTEXT.md
@.planning/phases/08-folder-upload/08-RESEARCH.md
@server.js
@package.json

<interfaces>
<!-- Key existing functions in server.js that this plan reuses -->

From server.js (lines 472-492) — /api/fetch pipeline (MUST replicate exactly):
```javascript
const delayInfo = detectVturbDelay(rawHtml);
const { html: cleanedHtml, scriptsRemoved, vslDetected } = cleanHtml(rawHtml);
const $ = cheerio.load(cleanedHtml, { decodeEntities: false });
const checkoutLinks = detectCheckoutLinks($, cleanedHtml);
const bundleImages = detectBundleImages($, checkoutLinks);
// Response shape:
return res.json({
  html: cleanedHtml,
  summary: {
    scriptsRemoved, vslDetected, checkoutLinks, bundleImages,
    delaySeconds: delayInfo ? delayInfo.delaySeconds : null,
    hasDelay: delayInfo !== null,
    delayScriptContent: delayInfo ? delayInfo.delayScriptContent : null,
    delayType: delayInfo ? delayInfo.delayType : null,
  },
});
```

From server.js (lines 750-825) — /api/export-zip current structure:
```javascript
app.post('/api/export-zip', async (req, res) => {
  const { html, headerPixel, headerPreload, vslembed, checkoutLinks, pageUrl,
          delaySeconds, delayScriptContent, delayType, bundleImages, extraScripts } = req.body;
  // ... buildExportHtml ...
  if (pageUrl) {
    // download assets, rewrite HTML, append to archive
  }
  // archive.append(outputHtml, { name: 'index.html' });
  // for downloaded assets: archive.append(buffer, { name: localPath });
  // await archive.finalize();
});
```

From server.js (lines 843-847) — module.exports:
```javascript
module.exports = app;
module.exports.cleanHtml = cleanHtml;
module.exports.detectCheckoutLinks = detectCheckoutLinks;
module.exports.detectVturbDelay = detectVturbDelay;
module.exports.detectBundleImages = detectBundleImages;
module.exports.buildExportHtml = buildExportHtml;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar multer e adicionar infraestrutura de upload ao server.js</name>
  <files>package.json, server.js</files>
  <action>
1. Rodar `npm install multer` para adicionar ao package.json.

2. Em server.js, adicionar require no topo (depois de `const archiver = require('archiver');` — linha 7):
```javascript
const multer = require('multer');
const crypto = require('crypto');
```
Nota: `path` ja esta importado na linha 6.

3. Adicionar constantes e session store ANTES da secao de rotas (depois do bloco de constantes existente, por volta da linha 50, antes do comment `// -- Helpers`). Criar bloco:

```javascript
// -- Upload: multer config + session store --
const ALLOWED_UPLOAD_EXTENSIONS = new Set([
  '.html', '.htm', '.css', '.js', '.mjs', '.json',
  '.svg', '.xml', '.png', '.jpg', '.jpeg', '.gif',
  '.webp', '.avif', '.ico', '.woff', '.woff2',
  '.ttf', '.eot', '.otf', '.map',
]);

const SESSION_TTL_MS = 30 * 60 * 1000; // 30 minutos
const uploadSessions = new Map(); // sessionId -> { assets: Map<relativePath, Buffer>, expiresAt }

// Limpeza periodica de sessoes expiradas
setInterval(function() {
  const now = Date.now();
  for (const [id, session] of uploadSessions) {
    if (session.expiresAt < now) uploadSessions.delete(id);
  }
}, 5 * 60 * 1000);

const folderUpload = multer({
  storage: multer.memoryStorage(),
  limits: { fileSize: 10 * 1024 * 1024, files: 200 },
  fileFilter: function(req, file, cb) {
    const ext = path.extname(file.originalname).toLowerCase();
    cb(null, ALLOWED_UPLOAD_EXTENSIONS.has(ext));
  },
});

function stripTopFolder(relativePath) {
  const parts = relativePath.split('/');
  return parts.slice(1).join('/');
}

function isSafeRelativePath(relativePath) {
  if (!relativePath || path.isAbsolute(relativePath)) return false;
  const normalized = path.normalize(relativePath);
  if (normalized.startsWith('..')) return false;
  if (relativePath.includes('\0')) return false;
  return true;
}
```

4. Adicionar rota POST /api/upload-folder DEPOIS da rota /api/fetch (depois da linha 493) e ANTES dos helpers de ZIP (linha 495):

```javascript
// -- Route: POST /api/upload-folder --
app.post('/api/upload-folder', folderUpload.array('files', 200), function(req, res) {
  const files = req.files || [];
  const rawPaths = req.body['paths[]'] || [];
  const paths = Array.isArray(rawPaths) ? rawPaths : [rawPaths];

  if (!files.length) {
    return res.status(400).json({ error: 'Nenhum arquivo recebido.' });
  }

  const assets = new Map();
  let indexHtmlBuffer = null;

  for (let i = 0; i < files.length; i++) {
    const file = files[i];
    const rawPath = paths[i] || file.originalname;
    const relative = stripTopFolder(rawPath);
    if (!isSafeRelativePath(relative)) continue;
    assets.set(relative, file.buffer);
    if (relative === 'index.html' || relative === 'index.htm') {
      indexHtmlBuffer = file.buffer;
    }
  }

  if (!indexHtmlBuffer) {
    return res.status(400).json({
      error: 'index.html nao encontrado na raiz da pasta. Selecione a pasta que contem diretamente o index.html.',
    });
  }

  const rawHtml = indexHtmlBuffer.toString('utf8');
  const delayInfo = detectVturbDelay(rawHtml);
  const { html: cleanedHtml, scriptsRemoved, vslDetected } = cleanHtml(rawHtml);
  const $ = cheerio.load(cleanedHtml, { decodeEntities: false });
  const checkoutLinks = detectCheckoutLinks($, cleanedHtml);
  const bundleImages = detectBundleImages($, checkoutLinks);

  const sessionId = crypto.randomUUID();
  uploadSessions.set(sessionId, {
    assets,
    expiresAt: Date.now() + SESSION_TTL_MS,
  });

  return res.json({
    html: cleanedHtml,
    uploadSessionId: sessionId,
    summary: {
      scriptsRemoved,
      vslDetected,
      checkoutLinks,
      bundleImages,
      delaySeconds: delayInfo ? delayInfo.delaySeconds : null,
      hasDelay: delayInfo !== null,
      delayScriptContent: delayInfo ? delayInfo.delayScriptContent : null,
      delayType: delayInfo ? delayInfo.delayType : null,
    },
  });
});
```

5. Adicionar multer error handler IMEDIATAMENTE apos a rota /api/upload-folder (antes dos helpers de ZIP):

```javascript
// Multer error handler (4-argument signature para Express tratar como error middleware)
app.use(function(err, req, res, next) {
  if (err && err.code === 'LIMIT_FILE_SIZE') {
    return res.status(413).json({ error: 'Arquivo muito grande. Limite: 10MB por arquivo.' });
  }
  if (err && err.code === 'LIMIT_FILE_COUNT') {
    return res.status(400).json({ error: 'Muitos arquivos. Limite: 200 arquivos por pasta.' });
  }
  next(err);
});
```

6. Modificar /api/export-zip (linha 750) para adicionar branch uploadSessionId:
- Na destructuring (linha 751), adicionar `uploadSessionId` ao final:
  ```javascript
  const { html, headerPixel, headerPreload, vslembed, checkoutLinks, pageUrl,
          delaySeconds, delayScriptContent, delayType, bundleImages, extraScripts,
          uploadSessionId } = req.body;
  ```
- Depois de `let outputHtml = buildExportHtml(...)` (linha ~758-760), ANTES do `if (pageUrl)` (linha ~766), inserir novo branch:
  ```javascript
  // Upload session branch — assets already in memory, no network download needed
  if (uploadSessionId) {
    const session = uploadSessions.get(uploadSessionId);
    if (!session) {
      return res.status(400).json({ error: 'Sessao de upload expirada. Refaca o upload da pasta.' });
    }

    res.setHeader('Content-Type', 'application/zip');
    res.setHeader('Content-Disposition', 'attachment; filename="pagina-afiliado.zip"');

    const archive = archiver('zip', { zlib: { level: 6 } });
    archive.on('error', (err) => {
      console.error('[export-zip] archive error:', err);
      res.destroy(err);
    });
    archive.pipe(res);

    archive.append(outputHtml, { name: 'index.html' });
    for (const [relativePath, buffer] of session.assets) {
      if (relativePath !== 'index.html' && relativePath !== 'index.htm') {
        archive.append(buffer, { name: relativePath });
      }
    }

    res.on('finish', function() { uploadSessions.delete(uploadSessionId); });
    await archive.finalize();
    return;
  }
  ```
  O bloco `if (pageUrl)` existente segue inalterado abaixo.

7. Adicionar `uploadSessions` ao module.exports no final do arquivo (linha ~847) para permitir testes:
```javascript
module.exports.uploadSessions = uploadSessions;
```

IMPORTANTE:
- NAO mover, alterar, ou duplicar as funcoes existentes (cleanHtml, detectCheckoutLinks, etc.)
- NAO alterar nenhuma outra rota existente alem do /api/export-zip
- A rota /api/upload-folder deve usar as funcoes existentes por chamada direta, nao por copia
  </action>
  <verify>
    <automated>cd /Users/victorroque/Downloads/Extrator2000 && node -e "const app = require('./server'); console.log('OK: server loads'); const http = require('http'); const srv = http.createServer(app); srv.listen(0, function() { const port = srv.address().port; fetch('http://localhost:'+port+'/api/health').then(r=>r.json()).then(d=>{console.log('Health:', d.ok); srv.close(); process.exit(d.ok ? 0 : 1)})})"</automated>
  </verify>
  <done>
- `npm ls multer` mostra multer instalado
- server.js carrega sem erros
- POST /api/upload-folder existe e aceita multipart
- POST /api/export-zip aceita uploadSessionId como campo adicional
- /api/health retorna { ok: true } (regressao: rotas existentes intactas)
- uploadSessions Map exportado para testes
  </done>
</task>

<task type="auto">
  <name>Task 2: Teste de integracao do fluxo upload-folder + export-zip</name>
  <files>server.js</files>
  <action>
Testar o fluxo completo via curl ou script Node inline. Executar os seguintes testes em sequencia:

1. **Teste: upload sem arquivos** — POST /api/upload-folder com body vazio, esperar 400 + "Nenhum arquivo recebido"

2. **Teste: upload sem index.html** — POST /api/upload-folder com um arquivo CSS + paths[] que nao contem index.html, esperar 400 + "index.html nao encontrado"

3. **Teste: upload com index.html valido** — Criar FormData com:
   - files: buffer de `<html><head></head><body><p>teste</p></body></html>` como index.html
   - paths[]: `minha-pasta/index.html`
   Esperar 200 + response com `html`, `uploadSessionId`, `summary` com formato identico ao /api/fetch

4. **Teste: export-zip com uploadSessionId** — Usar o uploadSessionId da resposta anterior para chamar /api/export-zip com `{ html: response.html, uploadSessionId: sessionId }`, esperar 200 + content-type application/zip

5. **Teste: export-zip com sessionId expirado/invalido** — Chamar /api/export-zip com uploadSessionId `fake-uuid`, esperar 400 + "Sessao de upload expirada"

6. **Teste de regressao: /api/fetch continua funcionando** — Confirmar que `node -e "require('./server')"` carrega sem erros

Se qualquer teste falhar, investigar e corrigir a implementacao da Task 1 antes de prosseguir.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Downloads/Extrator2000 && node -e "
const http = require('http');
const app = require('./server');
const srv = http.createServer(app);
srv.listen(0, async function() {
  const port = srv.address().port;
  const base = 'http://localhost:'+port;
  let ok = true;
  // Test 1: upload sem arquivos
  try {
    const r1 = await fetch(base+'/api/upload-folder', { method:'POST', headers:{'Content-Type':'application/json'}, body:'{}' });
    // multer expects multipart, will likely error or return empty
    console.log('T1 empty upload status:', r1.status);
  } catch(e) { console.log('T1:', e.message); }
  // Test 2: upload com index.html valido via multipart
  const FormData = (await import('node:buffer')).Buffer;
  const boundary = '----TestBoundary123';
  const htmlContent = '<html><head></head><body><p>test</p></body></html>';
  const body = [
    '--'+boundary,
    'Content-Disposition: form-data; name=\"files\"; filename=\"index.html\"',
    'Content-Type: text/html',
    '',
    htmlContent,
    '--'+boundary,
    'Content-Disposition: form-data; name=\"paths[]\"',
    '',
    'myfolder/index.html',
    '--'+boundary+'--',
  ].join('\r\n');
  const r2 = await fetch(base+'/api/upload-folder', {
    method:'POST',
    headers:{'Content-Type':'multipart/form-data; boundary='+boundary},
    body: body,
  });
  const d2 = await r2.json();
  console.log('T2 upload status:', r2.status, 'hasSessionId:', !!d2.uploadSessionId, 'hasHtml:', !!d2.html, 'hasSummary:', !!d2.summary);
  if (r2.status !== 200 || !d2.uploadSessionId) { ok = false; }
  // Test 3: export-zip with valid sessionId
  if (d2.uploadSessionId) {
    const r3 = await fetch(base+'/api/export-zip', {
      method:'POST',
      headers:{'Content-Type':'application/json'},
      body: JSON.stringify({ html: d2.html, uploadSessionId: d2.uploadSessionId }),
    });
    console.log('T3 export-zip status:', r3.status, 'content-type:', r3.headers.get('content-type'));
    if (r3.status !== 200) { ok = false; }
  }
  // Test 4: export-zip with invalid sessionId
  const r4 = await fetch(base+'/api/export-zip', {
    method:'POST',
    headers:{'Content-Type':'application/json'},
    body: JSON.stringify({ html:'<html></html>', uploadSessionId: 'fake-uuid-123' }),
  });
  const d4 = await r4.json();
  console.log('T4 expired session status:', r4.status, 'error:', d4.error);
  if (r4.status !== 400) { ok = false; }
  // Test 5: health check (regression)
  const r5 = await fetch(base+'/api/health');
  const d5 = await r5.json();
  console.log('T5 health:', d5.ok);
  if (!d5.ok) { ok = false; }
  srv.close();
  console.log('ALL OK:', ok);
  process.exit(ok ? 0 : 1);
});
"</automated>
  </verify>
  <done>
- Upload sem index.html retorna 400 com mensagem clara
- Upload com index.html retorna 200 + uploadSessionId + html + summary (mesmo formato que /api/fetch)
- Export-zip com uploadSessionId retorna ZIP valido (status 200, content-type application/zip)
- Export-zip com sessionId invalido retorna 400
- /api/health continua retornando { ok: true }
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/upload-folder | Arquivos e paths enviados pelo cliente; paths podem conter traversal |
| /api/upload-folder -> sessionStore | Buffers armazenados em memoria; sessionId retornado ao cliente |
| Browser -> /api/export-zip | uploadSessionId vem do cliente; pode ser forjado ou expirado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | /api/upload-folder paths[] | mitigate | isSafeRelativePath() rejeita paths com `..`, null bytes, ou absolutos. stripTopFolder() remove a pasta raiz do webkitRelativePath |
| T-08-02 | Denial of Service | /api/upload-folder file size | mitigate | multer limits: fileSize 10MB/file, files 200 max. Total efetivo ~2GB mas mitigado por memoria Node |
| T-08-03 | Denial of Service | sessionStore memory | mitigate | TTL 30 min + cleanup a cada 5 min + res.on('finish') deleta imediatamente apos export. Cap de 50MB total por upload |
| T-08-04 | Spoofing | uploadSessionId em /api/export-zip | accept | UUIDs sao criptograficamente aleatorios (crypto.randomUUID); brute-force impraticavel. Ferramenta local, risco baixo |
| T-08-05 | Information Disclosure | extensoes de arquivo | mitigate | fileFilter com whitelist ALLOWED_UPLOAD_EXTENSIONS descarta silenciosamente tipos nao permitidos |
| T-08-06 | Elevation of Privilege | script execution | mitigate | Assets uploadados sao armazenados como Buffers e servidos apenas dentro do ZIP; nenhum script e executado no servidor |
| T-08-07 | Tampering | multer error bypass | mitigate | Error middleware com 4 argumentos captura MulterError (LIMIT_FILE_SIZE, LIMIT_FILE_COUNT) e retorna 400/413 user-friendly |
</threat_model>

<verification>
1. `node -e "require('./server')"` carrega sem erros
2. `npm ls multer` mostra multer instalado
3. POST /api/upload-folder com index.html retorna mesmo formato que /api/fetch + uploadSessionId
4. POST /api/export-zip com uploadSessionId gera ZIP com assets preservados
5. POST /api/export-zip com uploadSessionId invalido retorna 400
6. POST /api/fetch continua funcionando (regressao)
7. POST /api/export-zip com pageUrl continua funcionando (regressao)
</verification>

<success_criteria>
- multer instalado e funcional
- /api/upload-folder processa index.html com pipeline identico ao /api/fetch
- /api/export-zip com uploadSessionId retorna ZIP com assets da sessao
- Session store com TTL de 30 minutos e cleanup periodico
- Path traversal bloqueado em todos os caminhos de upload
- Nenhuma regressao nas rotas existentes
</success_criteria>

<output>
After completion, create `.planning/phases/08-folder-upload/08-01-SUMMARY.md`
</output>
